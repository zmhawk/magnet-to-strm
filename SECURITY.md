# 安全政策

## 支持范围

项目目前处于早期阶段，只为最新发布版本和 `main` 分支提供安全修复。旧版本可能需要升级后才能获得修复。

## 报告漏洞

请不要通过公开 Issue 披露未修复的漏洞。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。如果该功能尚未启用，请联系仓库维护者，并仅提供足以建立安全沟通渠道的信息。

报告中请包含：

- 受影响的版本或提交；
- 复现条件和最小复现步骤；
- 可能造成的影响；
- 已知的缓解或修复建议；
- 是否已经向其他人披露。

维护者会尽量在 7 天内确认收到报告。修复发布前，请避免公开漏洞细节。

## 部署安全

- `.env`、`config.toml`、SQLite 数据库、日志和 STRM 可能包含凭据或私人内容信息，不应提交或公开分享。
- 必须设置高强度、唯一的 `MTS_ARIA2_RPC_SECRET`。
- 不建议把 `/jsonrpc`、`/redirect`、`/dav` 或管理端口直接暴露到公网；请使用防火墙、可信反向代理和访问控制。
- 115 Refresh Token 应按密码管理。怀疑泄露时应立即轮换，并检查运行日志和数据库副本。
- 容器镜像和第三方依赖应保持更新。
